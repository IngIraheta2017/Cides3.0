{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tiporecurso</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoRecurso.id }}</td>
            </tr>
            <tr>
                <th>Nombretipo</th>
                <td>{{ tipoRecurso.nombreTipo }}</td>
            </tr>
            <tr>
                <th>Descripciontipo</th>
                <td>{{ tipoRecurso.descripcionTipo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tiporecurso_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tiporecurso_edit', { 'id': tipoRecurso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
