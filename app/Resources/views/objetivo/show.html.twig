{% extends 'base.html.twig' %}

{% block body %}
    <h1>Objetivo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objetivo.id }}</td>
            </tr>
            <tr>
                <th>Descripcionobjetivo</th>
                <td>{{ objetivo.descripcionObjetivo }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('objetivo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('objetivo_edit', { 'id': objetivo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
